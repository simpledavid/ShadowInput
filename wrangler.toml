name = "shadow-input"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Pages output (used by OpenNext)
pages_build_output_dir = ".open-next/assets"

[[d1_databases]]
binding = "DB"
database_name = "shadow-input"
database_id = "fcc468fe-4846-494e-a3cb-535b1e5f3eaf"

[vars]
# Non-secret env vars can go here
# Secret env vars (API keys) must be set in Cloudflare Pages dashboard
NEXT_PUBLIC_APP_URL = "https://shadowinput.com"

